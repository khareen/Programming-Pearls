#include<stdio.h>
#include"vernacular.h"

void main()

{
	// string to integer conversion
	/*char str[10];	
	printf("Please enter a numerical string!");
	scanf("%s",str);
	strToInt(str)>0 ? printf("%d \n",strToInt(str)):printf("\n"); 
	*/
	// Integer to Binary conversion.
	/*
	int Int;
	printf("Enter an Integer");
	scanf("%d",&Int);
	intToBinary(Int);
	*/
}
